		
		#region "Association <association_name>"

		public bool _<association_name>Loaded {get;set;}

		/// <summary>
        /// Gets/Sets parent object
        /// </summary>
		public virtual <datatype> <association_name>()  
		    //1-1 parent association
            set {
                this._<association_name> = value
				if ( value != null ) {
					this.<child_field_runtime> = value.<parent_field_runtime>;
					AddHandler value.IDChanged, AddressOf this.handleParentIdChanged;
                } else {
					this.<child_field_runtime> = null;
				}

            }


            get {
                //LAZY LOADING! Only hit the database to get the child object if we need it
                if ( this._<association_name> == null ) Then
					this.load<association_name>();
                }
				
                return this._<association_name>;
            }
        }
        
        /// <summary>
        /// Loads parent object and sets the appropriate properties
        /// </summary>
        public virtual Sub load<association_name>() {
			
			If (this._<association_name>Loaded()) return;
			
			If ( this._<association_name> == null && this.<child_field_runtime> > 0 ) {
                
				//call the setter here, not the private variable!
                this.<association_name> = new <db_mapper>().FindByKey(this.<child_field_runtime>.Value);
                
            } // this._<association_name> is Nothing

            this._<association_name>Loaded=true;
			            
       }

		#endregion